rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT MODE: Allow all access for authenticated users
    // TODO: Re-enable security rules when ready for production
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
} 